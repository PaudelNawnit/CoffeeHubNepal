{
  "name": "coffeehubnepal",
  "version": "1.0.0",
  "description": "CoffeeHubNepal - Coffee Community Platform",
  "scripts": {
    "build": "cd apps/web && npm install && npm run build",
    "start": "cd apps/web && npm start",
    "postinstall": "cd apps/web && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

