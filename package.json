{
  "name": "coffeehubnepal",
  "version": "1.0.0",
  "description": "CoffeeHubNepal - Coffee Community Platform",
  "scripts": {
    "build": "cd apps/web && npm install --include=dev && npm run build",
    "start": "cd apps/web && npm start",
    "postinstall": "cd apps/web && npm install --include=dev"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=9.0.0"
  }
}

